name: Deploy Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: false

jobs:
  deploy-pages:
    permissions:
      actions: read
      contents: read
      pages: write
      id-token: write
      packages: read
    uses: moritzbrantner/reusable-workflows/.github/workflows/deploy-pages.yml@e0f92de2b9ae1ce715a19ab87bdd4486a5554139
    with:
      node_version: "24.20.0"
      bun_version: ""
      install_command: npm ci
      cache_bun: false
      cache_node: true
      npm_cache_dependency_path: package-lock.json
      build_command: npm run pages:build
      artifact_path: pages-dist
